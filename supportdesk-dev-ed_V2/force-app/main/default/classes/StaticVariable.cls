public class StaticVariable {
	public string name;
    public static integer age;
    

}

/*
StaticVariable sv = new StaticVariable();
sv.name = 'Gary';  
StaticVariable.age = 90; /here we can call static variable through the class name instead of reference variable 'sv'/

system.debug('name ='+sv.name);
system.debug('age ='+StaticVariable.age);
*/